import { ReplaceStringPipe } from './replace.string.pipe';

describe('Pipe: Replace.stringe', () => {
  it('create an instance', () => {
    let pipe = new ReplaceStringPipe();
    expect(pipe).toBeTruthy();
  });
});
